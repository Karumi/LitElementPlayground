language: node_js

node_js:
  - 10.8

addons:
  apt:
    packages:
      - libgconf-2-4

cache:
  # cache both npm modules and Cypress binary
  directories:
    - ~/.npm
    - ~/.cache

install:
  - npm ci

before_script:
  - npm run prod
  - npm start -- --silent &

script:
  - npm run lint
  - npm run cypress:run --record -key $RECORD_KEY
